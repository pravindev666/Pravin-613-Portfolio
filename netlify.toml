[build]
  base = "mathew-portfolio"
  command = "cd mathew-portfolio && npm install && npm run build"
  publish = "mathew-portfolio/.next"

[build.environment]
  NODE_VERSION = "18"

# Next.js plugin will handle routing automatically

